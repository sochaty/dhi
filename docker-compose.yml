services:
  server:
    build: ./server
    ports:
      - "8000:8000"
    environment:
      - CHROMA_HOST=chroma
      - OLLAMA_HOST=ollama
      - FIM_MODEL=${FIM_MODEL:-starcoder2:3b}
      - EMBED_MODEL=${EMBED_MODEL:-nomic-embed-text}
      - CHAT_MODEL=${CHAT_MODEL:-llama3.2:3b}
      - FIM_MODEL_MAX_TOKENS=${FIM_MODEL_MAX_TOKENS:-20}
      - OLLAMA_TIMEOUT=${OLLAMA_TIMEOUT:-120}
    depends_on:
      chroma:
        condition: service_started
      ollama:
        condition: service_healthy
    restart: unless-stopped

  chroma:
    image: chromadb/chroma:latest
    ports:
      - "8001:8000"
    volumes:
      - chroma_data:/chroma/chroma

  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    entrypoint: >
      /bin/sh -c "
        ollama serve &
        echo 'Waiting for Ollama to be ready...' &&
        until ollama list > /dev/null 2>&1; do sleep 2; done &&
        echo 'Pulling FIM model...' &&
        ollama pull ${FIM_MODEL:-starcoder2:3b} &&
        echo 'Pulling embed model...' &&
        ollama pull ${EMBED_MODEL:-nomic-embed-text} &&
        echo 'Pulling chat model...' &&
        ollama pull ${CHAT_MODEL:-llama3.2:3b} &&
        echo 'Models ready.' &&
        wait
      "
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 60s

volumes:
  chroma_data:
  ollama_models:
